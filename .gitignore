__pycache__/
*.pyc
.venv/
.venv*/
node_modules/
*.db
uploads/
.pytest_cache/
reports/*.log
docs/stack_*.log
# per-run stage artifacts written by service/orchestrator.save_stage_artifact
# (config.artifact_dir). One subdirectory per run_id, so this accumulates on
# every analysis and showed up as untracked noise on a clean checkout.
reports/artifacts/
dist/
.DS_Store

# transient local server logs (uvicorn / vite dev)
.backend.log
web/.dev-server.log
